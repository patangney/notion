'use client';

function Document({ id: _id }: { id: string }) {
  return (
    <div>
      <form>
        {/* update title... */}
        {/* isOwner && inviteUser, DeleteDocument */}
      </form>
      <div>
        {/* Manage Users */}
        {/* Avatars */}
      </div>
      {/* Collaborative Editro */}
    </div>
  );
}
export default Document;
